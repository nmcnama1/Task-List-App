<%= form_for(helpholder) do |f| %>
  <% if helpholder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(helpholder.errors.count, "error") %> prohibited this helpholder from being saved:</h2>

      <ul>
      <% helpholder.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :authoer %>
    <%= f.text_field :authoer %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
